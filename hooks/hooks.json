{
  "description": "Inject the source-attribution policy each turn (UserPromptSubmit — not SessionStart, so the policy survives compaction in long sessions, at a few tokens per turn) and verify filesystem-checkable citations both when Claude asks the user a question (PreToolUse/AskUserQuestion — Stop does not fire there) and when Claude finishes (Stop).",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/grounding_spec.py", "--emit-policy"]
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/grounding-verifier.py"]
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3",
            "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/grounding-verifier.py"]
          }
        ]
      }
    ]
  }
}
